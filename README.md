# Jellyfin Server

## Dependencies

* **Packer**

* **Vagrant**

* **VirtualBox**

## Install

**Run**: `cd packer && packer build ubuntu.pkr.hcl`

## Usage

**Run**: `vagrant up`

The jellyfin server will then be running on port `8096`